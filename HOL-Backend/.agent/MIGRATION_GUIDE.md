# 🗄️ دليل تحديث قاعدة البيانات - إضافة Department

بما أن التطبيق قيد التشغيل، لم نتمكن من تطبيق التحديثات مباشرة. يرجى اتباع الخطوات التالية لتحديث قاعدة البيانات:

## 1. إيقاف التطبيق
تأكد من إيقاف تشغيل الـ Backend API (أغلق الـ Terminal أو Visual Studio Debugger الذي يشغل التطبيق).

## 2. إنشاء Migration
افتح Terminal في مجلد المشروع `c:\CRM-V2\HOL-Backend` ونفذ الأمر التالي:

```bash
dotnet ef migrations add AddDepartmentToUser
```

## 3. تحديث قاعدة البيانات
بعد إنشاء الـ Migration بنجاح، نفذ الأمر التالي لتطبيق التغييرات على قاعدة البيانات:

```bash
dotnet ef database update
```

## ⚠️ حل بديل (SQL)
إذا واجهت مشاكل مع Entity Framework، يمكنك تنفيذ جملة SQL التالية مباشرة على قاعدة البيانات:

```sql
ALTER TABLE [users] ADD [department] nvarchar(max) NULL;
GO
```

---

**ملاحظة:** تم تحديث الكود بالكامل (Entity, DTOs, Services) ليدعم حقل `Department` الجديد.
